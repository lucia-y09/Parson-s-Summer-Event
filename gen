<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Random Prompt Generator</title>
  <style>
    body { font-family: sans-serif; display: flex; flex-direction: column; align-items: center; margin-top: 5em; }
    button { padding: 1em; font-size: 1.2em; margin-top: 1em; }
    #prompt { font-size: 1.5em; margin-top: 2em; max-width: 500px; text-align: center; }
  </style>
</head>
<body>
  <h1>🎲 Random Prompt Generator</h1>
  <button onclick="generatePrompt()">Get a Prompt</button>
  <div id="prompt"></div>

  <script>
    const prompts = [
      "Write a story set in the year 3000.",
      "Draw your dream house.",
      "Describe your perfect day.",
      "Invent a new holiday.",
      "What if animals could talk?",
      // Add more prompts here
    ];

    function generatePrompt() {
      const random = prompts[Math.floor(Math.random() * prompts.length)];
      document.getElementById('prompt').innerText = random;
    }

    // Optionally show one immediately on load
    generatePrompt();
  </script>
</body>
</html>
